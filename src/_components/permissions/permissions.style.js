import { StyleSheet } from 'react-native';
import { appgreen } from '../../_helpers';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: appgreen
  },
  image: {
    width: '100%',
    height: '70%',
    alignContent: 'center',
    justifyContent: 'center'
  }
});
