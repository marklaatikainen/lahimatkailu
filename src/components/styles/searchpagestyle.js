import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1
    },
    loading: {
        marginTop: 180,
        justifyContent: 'center',
        alignItems: 'center'
    },
    text: {
        fontSize: 15
    },
    notfound: {
        textAlign: 'center',
        justifyContent: 'center',
        marginTop: 70
    },
    bar: {
        flexDirection: 'row',
    },
})